# Автоматизация создания видео из презентаций PowerPoint с помощью n8n

## Описание

Этот пайплайн автоматически преобразует презентации PowerPoint (.pptx) с комментариями докладчика в профессиональные видеоролики с озвучкой. Система использует технологии TTS (Text-to-Speech) и автоматически синхронизирует длительность показа каждого слайда с продолжительностью его озвучки.

## Ключевые возможности

- 🎯 **Автоматическое создание видео** из презентаций PowerPoint
- 🎤 **Высококачественная озвучка** с помощью Coqui TTS
- ⏱️ **Автоматическая синхронизация** слайдов с речью
- 🐳 **Docker-based развертывание** для простоты установки
- 🔧 **Полностью автоматизированный процесс** без ручного вмешательства

## Архитектура системы

### Компоненты

1. **n8n** - оркестратор рабочих процессов
2. **Coqui TTS** - система синтеза речи
3. **FFmpeg** - обработка видео и аудио
4. **PostgreSQL** - база данных для n8n
5. **Python** - скрипты обработки презентаций

### Схема работы

```
PowerPoint (.pptx) → Извлечение слайдов и комментариев → TTS генерация → 
Создание видео для каждого слайда → Объединение в финальное видео
```

## Установка и запуск

### Предварительные требования

- Docker и Docker Compose
- Минимум 4GB RAM
- Минимум 10GB свободного места на диске

### Быстрый старт

1. **Клонируйте/загрузите проект**:
   ```bash
   # Скачайте все файлы проекта в рабочую директорию
   ```

2. **Запустите систему**:
   ```bash
   chmod +x start.sh
   ./start.sh
   ```

3. **Откройте n8n**:
   - Перейдите в браузере по адресу: http://localhost:5678
   - Создайте учетную запись администратора

4. **Импортируйте пайплайн**:
   - В n8n выберите "Workflows" → "Import from File"
   - Загрузите файл `n8n_presentation_pipeline.json`

### Ручная установка

Если автоматический скрипт не работает:

1. **Создайте директории**:
   ```bash
   mkdir -p shared/{slides,audio,videos,uploads}
   mkdir -p scripts
   ```

2. **Запустите Docker Compose**:
   ```bash
   docker-compose build
   docker-compose up -d
   ```

3. **Проверьте статус сервисов**:
   ```bash
   docker-compose ps
   ```

## Использование

### Через интерфейс n8n

1. Откройте n8n по адресу http://localhost:5678
2. Перейдите к импортированному пайплайну "Presentation to Video Pipeline"
3. Нажмите кнопку "Execute Workflow"
4. Загрузите презентацию PowerPoint через первый узел
5. Дождитесь завершения обработки
6. Готовое видео будет сохранено в `/shared/final_presentation.mp4`

### Требования к презентации

- **Формат**: `.pptx` (PowerPoint 2007 и новее)
- **Комментарии докладчика**: обязательны для каждого слайда
- **Размер файла**: рекомендуется до 50MB
- **Количество слайдов**: рекомендуется до 50 слайдов

### Пример структуры комментариев

Комментарии докладчика должны содержать текст, который будет озвучен:

```
Добро пожаловать на нашу презентацию. 
Сегодня мы рассмотрим новые возможности автоматизации.
Этот слайд показывает основные преимущества нашего решения.
```

## Конфигурация

### Настройка TTS

По умолчанию используется русская модель TTS. Для изменения языка отредактируйте файл `docker-compose.yml`:

```yaml
coqui-tts:
  command: >
    python3 -m TTS.server.server 
    --model_name tts_models/en/ljspeech/tacotron2-DDC  # для английского
    --host 0.0.0.0 
    --port 5002
```

### Настройка качества видео

В пайплайне можно изменить параметры FFmpeg для настройки качества:

- **Разрешение**: изменить параметры `-s 1920x1080`
- **Битрейт**: изменить параметр `-b:v 2000k`
- **Кодек**: по умолчанию используется H.264 (`libx264`)

## Структура проекта

```
.
├── n8n_presentation_pipeline.json  # Основной пайплайн n8n
├── presentation_demo.pptx          # Демо-презентация
├── speaker_notes.json             # Извлеченные комментарии
├── docker-compose.yml             # Конфигурация Docker
├── Dockerfile                     # Кастомный образ n8n
├── start.sh                      # Скрипт быстрого запуска
├── extract_presentation.py       # Извлечение данных из PPTX
├── slides_to_images.py           # Конвертация слайдов в изображения
├── generate_tts.py               # Генерация TTS аудио
└── shared/                       # Рабочие файлы
    ├── slides/                   # PNG изображения слайдов
    ├── audio/                    # WAV аудио файлы
    ├── videos/                   # MP4 видео слайдов
    └── final_presentation.mp4    # Готовое видео
```

## Устранение неполадок

### Проблемы с запуском

1. **Docker не запускается**:
   ```bash
   sudo systemctl start docker
   docker-compose ps
   ```

2. **Недостаточно памяти**:
   - Увеличьте память для Docker (минимум 4GB)
   - Закройте другие приложения

3. **Ошибки портов**:
   ```bash
   # Проверить занятые порты
   netstat -tlnp | grep :5678
   netstat -tlnp | grep :5002
   ```

### Проблемы с обработкой

1. **TTS не работает**:
   ```bash
   # Проверить статус Coqui TTS
   curl http://localhost:5002/docs
   docker-compose logs coqui-tts
   ```

2. **Презентация не обрабатывается**:
   - Убедитесь, что файл имеет формат .pptx
   - Проверьте наличие комментариев докладчика
   - Проверьте логи: `docker-compose logs n8n`

3. **Видео не создается**:
   ```bash
   # Проверить FFmpeg
   docker exec -it <n8n_container_id> ffmpeg -version
   ```

## Производительность

### Время обработки (примерно)

- **1 слайд с 50 словами**: ~30 секунд
- **10 слайдов презентация**: ~5-10 минут
- **50 слайдов презентация**: ~20-30 минут

### Требования к ресурсам

- **RAM**: 4-8GB (в зависимости от размера презентации)
- **CPU**: 2-4 ядра (TTS требует вычислительных ресурсов)
- **Диск**: 10-50GB (временные файлы)

## Лицензия и поддержка

Этот проект создан для демонстрации возможностей автоматизации с помощью n8n. 

### Компоненты с открытым исходным кодом:
- n8n (Apache 2.0)
- Coqui TTS (Mozilla Public License 2.0)
- FFmpeg (LGPL/GPL)
- Python-pptx (MIT)

### Поддержка
- Документация n8n: https://docs.n8n.io/
- Coqui TTS: https://github.com/coqui-ai/TTS
- FFmpeg: https://ffmpeg.org/documentation.html

## Примеры использования

### Корпоративные презентации
- Автоматическое создание видео-презентаций для сотрудников
- Массовая обработка обучающих материалов
- Создание видео-контента для внутренних порталов

### Образование
- Преобразование лекционных материалов в видео
- Создание онлайн-курсов из PowerPoint презентаций
- Автоматизация создания образовательного контента

### Маркетинг
- Быстрое создание видео для социальных сетей
- Автоматизация создания промо-роликов
- Преобразование презентаций в видео-контент

---

**Версия**: 1.0
**Дата создания**: Сентябрь 2025
**Автор**: Автоматизированный пайплайн n8n